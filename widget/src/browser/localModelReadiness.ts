export function localModelReadinessFailureMessage(): string {
  return "Talent Advisor's local model is not ready. Start Ollama and confirm qwen3:8b is installed, then open Talent Advisor again. No profile or application was created.";
}
